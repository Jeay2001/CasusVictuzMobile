<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CasusVictuzMobile.MainPage"
             xmlns:local="clr-namespace:CasusVictuzMobile.MVVM.View"
             BackgroundColor="#ffffff">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <VerticalStackLayout Grid.Row="0" Padding="20" Spacing="10">
            <!-- Header -->
            <HorizontalStackLayout VerticalOptions="Start" Spacing="10">
                <Label Text="Evenementen" FontSize="28" FontAttributes="Bold" TextColor="Black" />
                <Image Source="sort.png" HeightRequest="30" WidthRequest="30" HorizontalOptions="EndAndExpand" />
                <Image Source="filter.png" HeightRequest="30" WidthRequest="30" />
            </HorizontalStackLayout>

            <!-- Event Cards -->
            <ScrollView>
                <StackLayout>
                    <Label Text="Upcoming Events" 
               FontSize="24" 
               HorizontalOptions="Center" />

                    <CollectionView ItemsSource="{Binding FutureAcceptedEvents}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame BorderColor="Gray" CornerRadius="10" Padding="10" Margin="5">
                                    <Grid ColumnDefinitions="2*, 3*" RowDefinitions="Auto, Auto">
                                        <!-- Afbeelding -->
                                        <Image Source="{Binding PictureLink}" Aspect="AspectFill" HeightRequest="100" WidthRequest="100" Grid.Column="0" VerticalOptions="Center" />

                                        <!-- Tekstuele informatie -->
                                        <StackLayout Grid.Column="1" Spacing="5">
                                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                            <Label Text="{Binding Date, StringFormat='{}{0:MMMM dd, yyyy}'}" FontSize="14" TextColor="Gray" />
                                            <Label Text="{Binding Location.Name}" FontSize="14" TextColor="DarkGray" />
                                            <Label Text="{Binding SpotsInfo}" FontSize="14" TextColor="DarkGray" />

                                            <!-- Registratieknop -->
                                            <Button Text="{Binding RegistrationButtonText}"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ToggleRegistrationCommand}"
                                CommandParameter="{Binding .}"
                                BackgroundColor="{Binding RegistrationButtonColor}" />
                                        </StackLayout>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                    
                </StackLayout>
            </ScrollView>
        </VerticalStackLayout>

        <local:NavBar Grid.Row="1" />
    </Grid>
</ContentPage>
