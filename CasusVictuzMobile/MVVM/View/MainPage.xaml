<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CasusVictuzMobile.MainPage"
             xmlns:local="clr-namespace:CasusVictuzMobile.MVVM.View"
             BackgroundColor="#ffffff">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollView>
            <VerticalStackLayout Grid.Row="0" Padding="20" Spacing="10">
                <!-- Header -->
                <FlexLayout Direction="Column">
                    <Label Text="Evenementen" FontSize="28" FontAttributes="Bold" TextColor="Black" />

                    <!-- Separator -->
                    <BoxView HeightRequest="0.5" Color="LightGray" Margin="0,5,0,5"/>

                    <!-- Filter and Sort Container -->
                    <HorizontalStackLayout HorizontalOptions="CenterAndExpand" Spacing="10">
                        <!-- Filter Section -->
                        <HorizontalStackLayout Spacing="5">
                            <HorizontalStackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenFilterCommand}" />
                            </HorizontalStackLayout.GestureRecognizers>
                            <Image Source="filter.png" HeightRequest="30" WidthRequest="30" Margin="5,0,0,0"/>
                            <Label Text="Filter" FontSize="20" FontAttributes="Bold" TextColor="Black" VerticalOptions="Center"/>
                        </HorizontalStackLayout>

                        <!-- Sort Section -->
                        <HorizontalStackLayout>
                            <HorizontalStackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OpenSortCommand}" />
                            </HorizontalStackLayout.GestureRecognizers>
                            <Image Source="sort.png" HeightRequest="30" WidthRequest="30" Margin="5,0,0,0"/>
                            <Label Text="Sort" FontSize="20" FontAttributes="Bold" TextColor="Black" VerticalOptions="Center"/>
                        </HorizontalStackLayout>


                    </HorizontalStackLayout>



                    <!-- Separator -->
                    <BoxView HeightRequest="0.5" Color="LightGray" Margin="0,5,0,5"/>
                    <Button Text="Maak suggestie" x:Name="SuggestionButton" Command="{Binding OpenSuggestionCommand}" BackgroundColor="#2596be" TextColor="Black" HorizontalOptions="Center" />
                </FlexLayout>



                <!-- Event Cards -->

                <StackLayout>
                    <Label Text="Upcoming Events" 
               FontSize="24" 
               HorizontalOptions="Center" />

                    <CollectionView ItemsSource="{Binding DisplayedEvents}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame BorderColor="Gray" CornerRadius="10" Padding="10" Margin="0,10" BackgroundColor="LightBlue" >
                                    <Grid ColumnDefinitions="2*, 3*" RowDefinitions="Auto, Auto">
                                        <!-- Afbeelding -->
                                        <Image Source="{Binding PictureLink}" Aspect="AspectFill" HeightRequest="100" WidthRequest="100" Grid.Column="0" VerticalOptions="Center" Margin="5,0,5,0"/>

                                        <!-- Tekstuele informatie -->
                                        <StackLayout Grid.Column="1" Spacing="5">
                                            <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" TextColor="Black" />
                                            <Label Text="{Binding Date, StringFormat='{}{0:MMMM dd, yyyy}'}" FontSize="14" TextColor="Black" />
                                            <Label Text="{Binding Location.Name}" FontSize="14" TextColor="DarkGray" />
                                            <Label Text="{Binding SpotsInfo}" FontSize="14" TextColor="DarkGray" />

                                            <!-- Registratieknop -->
                                            <Button Text="{Binding RegistrationButtonText}"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.ToggleRegistrationCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="{Binding RegistrationButtonColor}" />

                                            <Button
                                                Text="Details"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.NavigateToDetails}"
                                                CommandParameter="{Binding .}" />
                                        </StackLayout>
                                        
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                </StackLayout>

            </VerticalStackLayout>
        </ScrollView>

        <local:NavBar Grid.Row="1" />
    </Grid>

</ContentPage>
